#include <stdio.h>

#define ROW_SIZE = 5
#define Board = int[ROW_SIZE*ROW_SIZE]

int main () {
    Board test;

    for ( int i = 0; i < ROW_SIZE; i++ ) {
        // int sq;
        // scanf("%d", &sq);
        // test[i] = sq;
    }
    return 0;
}